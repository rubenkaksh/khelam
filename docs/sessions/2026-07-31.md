# Session — 2026-07-31

## Objective
- Establish the daily session file as Hard Rule #1 (project AGENTS.md) so every future session starts by preparing `docs/sessions/YYYY-MM-DD.md`.
- Close out the booking bottom sheet iteration (7 tasks) that was carried over from the prior session.
- Build the real API data layer for the schedule/booking view: DTO models + repository-to-API service following the app's pattern, using `lib/ui/features/schedule/data/dummy.dart` as the API response reference.

## Work Log
- Reviewed the anchored summary from the booking bottom sheet iteration (design docs dated 2026-07-28, commits up to `9c6ea6f`).
- Clarified rule scope/convention with user: project-only, `docs/sessions/YYYY-MM-DD.md`, create at session start and update continuously.
- Added **Hard Rule #1 — Daily Session File** to `/Users/rubenk/projects/khel-service/khelam/AGENTS.md` (top of file, above Strict Rules) with the session template.
- Created this file: `docs/sessions/2026-07-31.md`.
- Prior-session context (booking bottom sheet, all tasks done, 44/44 tests passing):
  - `7b88cd0` FormBottomSheet, `bd24f79` PhoneInput, `3900569` customerPhone model field
  - `7576731` service layer, `7b4f56a` BookingConfirmationSheet, `22b4c78` view/cubit wiring, `9c6ea6f` docs
- **API data layer task (asked after session file rule was in place):**
  - User decisions: Flutter side only (no backend edits); base URL via flutter_dotenv; no JWT wiring yet (book → 401 until auth lands); dummy turf data. Dev turf id `44444444-4444-4444-4444-444444444441`.
  - Backend recon (`rms-futsal-backend`): `GET /slots?turfId=&date=` public, returns `{id, slot_date, start_time, end_time, status, isBooked, bookedBy}` (no `turf_id` per row); `POST /slots/:id/book` JWT-guarded, identifies user via token (no customerPhone param), returns `{booking, slot:{id,status}}`; money = Prisma Decimal → JSON strings; times = `1970-01-01THH:MM:SS.000Z` (wall clock) + `slot_date`; bookings.status is a free string (default "pending"); bookings table has NO customer_phone column; no turfs endpoint; default port 5000.
  - Added `flutter_dotenv: ^6.0.1`; created `.env.example` + `.env` (kept committed — it is now a pubspec asset so it must exist at build time; values are non-secret dev defaults).
  - Extended `DioApiClient` with `getJsonList` (query params) + `postJson` (+ `ApiClientException`); reuses existing 10s timeouts.
  - `BookingApiService` (implements `BookingService`): getTurf → dummy turf; getSchedule → GET /slots; bookSlot → POST /slots/:id/book (no body, customerPhone intentionally not sent).
  - `BookingService.bookSlot` → `Future<void>`; repository + `MockBookingService` updated (mock records booked slot ids).
  - `ScheduleCubit`: default turf id = dev turf; `bookSlot` refetches the schedule after booking (server = source of truth); stats count by `SlotStatus.booked` so API data (no booking object) is counted.
  - `BookingTimeline` booked-card check now `item.booking != null || slot.status == booked`.
  - DI: `lib/di/env_config.dart` helper (`envValue` guards `dotenv.isInitialized` — plain `dotenv.env` throws NotInitializedError in widget tests); `DataDependencies` base URL from `.env`; `ScheduleDependencies` mock/API switch (`USE_MOCK_BOOKING=='true'` or no `API_BASE_URL` → mock).
  - `main.dart` loads `dotenv.load(isOptional: true)`; `.env` registered in pubspec assets.
- **Parsing refactor (user correction):** domain models in this repo are ALREADY freezed + json_serializable with generated `fromJson` (`Slot`, `Booking`, `ScheduleSlotItem`, `TurfSummary`, …). The first pass added a redundant DTO layer; per user's steer it was removed:
  - `Slot` + `Booking` now carry `@JsonKey(name: 'slot_date' | 'start_time' | 'end_time' | 'turf_id' | 'booking_code' | 'user_id' | 'slot_id' | ...)` mapping the API's snake_case keys, plus a top-level `_amountToDouble` converter on the three money fields of `Booking` (top-level required — an in-class static breaks freezed's generated copyWith).
  - `// ignore_for_file: invalid_annotation_target` on both model files (JsonKey is field/getter-targeted; analyzer flags constructor params — freezed moves them to fields).
  - `BookingApiService` parses the raw JSON with `Slot.fromJson`, completing the payload with `turf_id` (list endpoint omits it per row: `{...json, 'turf_id': turfId}`), then recombines `slot_date` + wall-clock times into local DateTimes via `copyWith`; `bookedBy` → `customerName`. Book response body unused (cubit refetches).
  - Deleted `lib/data/models/` (SlotDto/BookingDto + generated) and the DTO tests; added `test/domain/models/slot_test.dart` + `booking_test.dart` (snake keys, string Decimal amounts, enum mapping).

## Decisions
- Rule lives in the **project** AGENTS.md only (not global) — user chose khelam-scoped.
- Convention: `docs/sessions/YYYY-MM-DD.md`, one dated file per day, kept as history.
- Maintenance: create at the start of the first session of the day; update continuously (after each task/decision/commit); read it at the start of every session.
- Booking iteration design decisions (from prior session, still valid): 10-digit mobile (`^[6-9]\d{9}$`), `+977` prefix (amended 2026-08-07 from `+91`), `customerPhone` on Booking, atomic common widgets in `lib/ui/common/` for the forkable parent.
- API layer decisions:
  - `.env` stays committed (pubspec asset requirement; contents are dev-only defaults, no secrets).
  - Env access goes through `envValue` (lib/di/env_config.dart) so widget tests never hit `NotInitializedError`.
  - Booked-ness on the schedule screen derives from `slot.status` (list endpoint has no booking object); `bookedBy` supplies the customer name.
  - Booking keeps returning `void` from the service; the cubit refetches — avoids stale partial data from the book response (which lacks slot times).
  - **JSON parsing uses the domain models' generated `fromJson` (freezed + json_serializable), the repo convention — no DTO layer.** API quirks are absorbed at the boundary: per-field `@JsonKey(name:)` for snake_case keys (mixed with camel `isBooked`/`bookedBy`, which stay unannotated/unused), `_amountToDouble` converter for string Decimals, the service completing `turf_id` and recombining dummy-date times.
  - `Slot.fromJson`/`Booking.fromJson`/`toJson` wire format is now snake_case where annotated — no existing callers serialize these models, so no regression.

## Blockers
- None for the Flutter side. Reminder: `POST /slots/:id/book` returns 401 until real auth (JWT) is wired — booking shows "Could not book slot." with mock auth.
- TODO on backend: turfs endpoint (`getTurf` returns hardcoded dummy data until it exists).

## Next Steps
- Restart opencode so Hard Rule #1 is loaded into the session prompt (config-time files are not hot-reloaded).
- Commit this batch: `AGENTS.md`, `docs/sessions/`, `docs/superpowers/` (plan + specs from prior iteration), `.env`/`.env.example`, DTOs, api service, DI changes, tests.
- Next feature work: real auth/JWT wiring (unlocks booking); turfs endpoint (backend) + `getTurf` real call.
