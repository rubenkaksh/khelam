# Session — 2026-08-03

## Objective
Debug the Google sign-in `idToken` issue on the Android emulator (handoff Issue 2: login succeeds but `idToken` reported null) and fix VS Code breakpoints in commons package sources ("unverified"). Then: exercise the backend auth endpoints (`register`/`login`) per the API reference.

## Work Log
(Google sign-in + breakpoint work — see items below; then backend auth flow:)
- User requested POST :5000/auth/users/register + /login. **:5000 is Apple AirPlay (ControlCenter/AirTunes), not the backend** — backend `.env` has `PORT=8000`; was not running.
- `rms-futsal-backend` (NestJS + Prisma + Supabase pooler DB at `aws-1-ap-south-1.pooler.supabase.com`): on `main`, auth is OTP-based (send-otp/verify-otp) with NO register/login routes (confirmed via `git log -S "users/register"`).
- User: "do a git fetch and switch to this branch feature/login-auth" → fetched, switched (local mods `src/config/env.config.ts`, `src/slots/*` carried over). Branch adds `POST /auth/users/register` + `users/login` (DTOs: `UserRegisterDto{phoneNumber 10-15 digits, fullName, password ≥6}`, `UserLoginDto`), service = bcrypt hash/compare + JWT `{sub, role:"user", phoneNumber}`, `{accessToken, user}` responses; JWT secret falls back to dev string; schema adds `users` model (full_name, password_hash, etc.).
- `prisma db push` REFUSED (would drop `slots.booked_by` (3 vals) / `updated_at` (127 vals) + `migration_history`) — nothing destructive done; DB already had the schema (users table present).
- **tsx DI bug**: `npm run start` (tsx 4.23.1) → every endpoint 500 `Cannot read properties of undefined (reading 'userRegister'/'userLogin'/'$queryRaw')` — esbuild doesn't emit decorator metadata → Nest injects nothing. Fixed by running `npm run start:dev` (nest start --watch, real tsc honors `emitDecoratorMetadata`).
- Executed: register → **409** "Phone number already registered" (user `d3888e33-dcaa-4cc5-a08a-84efdb0cd999`, created 2026-08-03T09:54 — earlier today); login → **201** `{accessToken: <JWT HS256 7d>, user: {...}}`. `/health` → db connected.
- Server still running in background (`npm run start:dev`, log `/tmp/khel-backend-dev.log`, port 8000).
- `flutter build apk --debug` in khelam: ✓ `build/app/outputs/flutter-apk/app-debug.apk` (Gradle 22.8s) — compiles with commons as path dep. `.env` `API_BASE_URL=http://10.0.2.2:8000` matches the now-running backend.

## Decisions
- User confirmed: debug launch via Run button → "khelam (debug)" config; null-idToken observation was **pre-cleanup (stale)** — consistent with the code trace.
- ROOT CAUSE of unresolved breakpoints: `dart.debugExternalLibraries` is **deprecated/ignored** in current Dart Code (renamed to `dart.debugExternalPackageLibraries`, per maintainer DanTup in Dart-Code#4724). The launch.json flag never applied.
- Fix: `.vscode/settings.json` → `"dart.debugExternalPackageLibraries": true` (documented, applies to all launches); launch.json cleaned (removed dead key, kept minimal config).
- Backend: use port **8000** (NOT 5000 — AirPlay); use `npm run start:dev`, not `npm run start` (tsx DI broken).

## Blockers
- Need user to reload VS Code window, restart debug session, re-test breakpoints in commons.
- Need user to sign in once on the fresh build and report the `[khelam-debug] RAW AUTH` line (expected: idToken with length > 0 → Issue 2 stale).
- Register happy-path (201) not demonstrated — phone already registered; would need a fresh test number (ask user before creating test data in Supabase).

## Next Steps
1. User: reload VS Code window (applies settings.json), close any commons tabs opened from `~/.pub-cache/git/commons-*` (stale copies), restart debug via "khelam (debug)" → breakpoints in `/Users/rubenk/projects/commons` should now resolve (status bar should read "Debug my code + packages").
2. User signs in on the emulator; report `[khelam-debug] RAW AUTH: email=... idToken=<N> chars` from the debug console.
3. If idToken present (expected): close Issue 2 as stale; remove the temporary instrumentation from commons; keep the `AuthApiService` guard as a safety net.
4. If idToken NULL: chain cannot produce that silently — instrument deeper at the pigeon boundary / capture native-side `GoogleIdTokenCredential` contents via logcat; check for build-version drift (running APK ≠ current source).
5. Backend: optionally demonstrate register 201 with a fresh test phone number; consider fixing the `start` script (tsx) — e.g. document start:dev, or configure esbuild decorator metadata workaround.
