# Session — 2026-08-01

## Objective
- Architecture review from the beginning of git history (first commit `946db2e` → HEAD `22b4c78`, 24 commits): verify feature-first structure is being implemented properly (per the improve-codebase-architecture skill).
- Produce HTML report of deepening candidates; list all shortcomings in this session file.
- **Execute the top recommendation**: C2 (delete pass-through repositories) → C1 (feature collapse into `lib/features/<feature>/`, reopening ADR-0002/0003) with C3 (dead template remnants), C6 (per-route DI), C4 (DayStats) and minor findings riding along.

## Work Log
- Read: ADR-0001/0002/0003, prior `docs/architecture-review.md` (2026-06-22), `docs/commons.md`, prior session file (2026-07-31). No CONTEXT.md existed.
- Walked full git history (24 commits); read every module in `lib/` (~40 files); verified: analyze clean, `dummy.dart` unimported, `PaginatedListCubit` zero callers, empty `lib/domain/use_cases/` + `lib/ui/features/home/view_models/`.
- Wrote + opened HTML report: `/var/folders/3_/p2_f0b4d38z78kmmhnj8sd840000gn/T/architecture-review-1785564991.html`
- **Implementation (uncommitted, working tree):**
  - **C2 — deleted pass-through repos**: `BookingRepository`, `AuthRepository`, `TemplateRepository`, `SampleStatusRepository`, `LocalSampleRepository` (whole `lib/data/repositories/`). Cubits now take service interfaces directly (`ScheduleCubit(service:)`, `AuthCubit(service:)`); `HomeCubit` deleted (home became a placeholder).
  - **C1 — feature modules**: `git mv` booking slice → `lib/features/booking/` (models + freezed/g, `booking_service.dart` interface, `data/{mock,api}_booking_service.dart`, `bloc/schedule_cubit.dart`, `views/`, `widgets/`, `di/booking_dependencies.dart`); auth → `lib/features/auth/` (incl. `auth_user` model + `mock_auth_service`); home → `lib/features/home/` (placeholder view, no cubit); theme_preview → `lib/features/theme_preview/`; `DioApiClient` → `lib/core/network/`. `lib/domain/` and `lib/data/` deleted entirely.
  - **C3 — deleted template remnants**: `lib/features/presentation/` (PaginatedListCubit), paginated/sample/local_sample/template chains (models, interfaces, services, registrations), `template_info` chain; `dummy.dart` (unimported) removed; empty dirs cleaned.
  - **C6 — DI coordination**: deleted `app_dependencies.dart` + `data_dependencies.dart`; `service_locator.dart` is now aggregator-only (DioApiClient + feature registries + AppRouter); `app.dart` takes just the router (no MultiBlocProvider); each route builds its own `BlocProvider` from its feature registry; `main.dart` slims to `configureDependencies()` + router.
  - **C4 — DayStats** moved from `widgets/day_stats_section.dart` into `bloc/schedule_cubit.dart` (state vocabulary beside state machine); widget imports the cubit. Revenue `?? 0` behavior kept with a comment (API list endpoint has no booking payload; backend TODO).
  - **Minor findings**: 11 hand-written `!` usages rewritten null-aware (feedback ×4, status_badge, section_header, login_view, schedule_view ×2, booking_timeline ×2, turf_header); new `test/features/booking/data/mock_booking_service_test.dart` (mock adapter was untested); test tree mirrored (`test/features/booking/{models,data,widgets}`, `test/core/network/`, deleted `test/features/pagination/` + sample/local_sample repo tests); `test/.DS_Store` removed; `test/README.md` updated to new structure.
  - **Docs**: `docs/adr/adr-0004-feature-modules-own-their-slice.md` (new ADR, supersedes parts of 0002/0003 — added "Superseded in part" notes to both); `CONTEXT.md` created at repo root (domain glossary: Turf, Slot, Schedule, Booking, Book slot, DayStats, Auth/demo account, Feature module).
- **Verification**: `flutter analyze` clean; `flutter test` 51/51 pass (was 44). `dart format` run; format-only churn on untouched files reverted (buttons/inputs/stat_card/phone_input/bottom_sheet/theme/env_config + 2 tests) to keep the diff focused.
- **Committed**: `601ec7b refactor(arch): collapse features into modules, drop pass-through repos` — 96 files changed, +751/−1849 (renames tracked cleanly via git mv).
- **Committed**: `30d982f docs(sessions): record 2026-08-01 architecture review and refactor`; `a7a4a5b docs: add backlog from architecture review and 2026-07-31 session` (`docs/backlog.md`: C5 adapter drift, revenue ₹0 API-mode, real home, JWT auth, turfs endpoint, theme-monolith watchpoint).
- **Grill-with-docs on `docs/commons-migration-agent-instructions.md`** (delegated to architect): updated the 9-phase commons-extraction plan to match post-ADR-0004 reality — scan paths now target `lib/ui/common/**` (atomic widget catalog, `docs/commons.md` is the manifest), `lib/core/**`, `lib/ui/core/**`, `lib/ui/navigation/**`; skip `lib/features/**` with ADR-0004 rationale; rename-aware "already in base?" comparison (`git log --follow` / `git diff --find-renames` — naive relative-path diff breaks after `601ec7b` renames); subtree-split/filter-repo examples point at real paths; "forkable parent" vs "customized app" (khelam) terminology. Uncommitted. Open questions for user: (1) include `DioApiClient` in first commons extraction or keep separate? (2) is base repo available locally for rename-aware comparison? (3) keep "package" wording (Flutter tooling sense)?

## Decisions
- **Feature modules own their vertical slice** (ADR-0004): `lib/features/<feature>/` holds models, service interface, adapters, bloc, views, widgets, DI. Shared infra stays global (`lib/core/network/`); atomic widgets in `lib/ui/common/`; theming `lib/ui/core/`; navigation `lib/ui/navigation/`.
- **Pass-through repositories are banned** (extends ADR-0001): cubits depend on the service interface; only repositories with real logic return.
- **Routes provide their own cubit** via `BlocProvider` from the feature's DI registry; `app.dart` no longer lists features.
- **Home = placeholder** (login still routes there); meta-demo content deleted with the template chain.
- **Mock adapter behavior unchanged** (still ignores requested turfId — C5 adapter-drift was NOT in the top recommendation scope; noted for a future pass).
- **Revenue stat stays booking-payload-based** (₹0 in API mode) — backend must supply booking details on list slots; documented in code + CONTEXT.md.
- Kept the `!`-free strict rule: all rewritten usages use `if case` typed patterns / `?.` / `??`.

## Blockers
- None. Reminders carried over: `POST /slots/:id/book` 401s until JWT auth is wired; turfs endpoint TODO (getTurf hardcoded); revenue ₹0 in API mode until backend includes booking on list slots.

## Next Steps
- Review + commit this batch (uncommitted: ~50 renames/deletions + rewrites + docs). Suggest a single `refactor(arch): feature modules own their slice (ADR-0004)` commit or split into (a) deletions, (b) moves, (c) docs.
- Future: C5 adapter drift (turf id ownership), real auth/JWT, turfs endpoint, real home experience.

## Commons-Migration Grill (architect)
Ran a grill-me stress test against `docs/commons-migration-agent-instructions.md`. Evidence gathered:
- Base repo is NOT in `~/dev`; it lives at `/Users/rubenk/Projects/forkable` (remote `forkable_flutter.git`), actively maintained, pre-ADR-0004 layered layout (DioApiClient at `lib/data/services/`, only 4/9 common widgets, no `test/ui/common/`).
- khelam sits at `~/projects/khel-service/khelam` — sibling to forkable only via coincidence of `$HOME` children, NOT a shared parent. Phase 6's "all three siblings" mandate is violated on this machine.
- All 9 `lib/ui/common/` widgets import only flutter/khelam-ui (zero app coupling) → clean first slice.
- Only `test/ui/common/{bottom_sheet,phone_input}_test.dart` exist (2 of 9), not the full catalog.
- Web (2026): for separate repos, git-dep + `pubspec_overrides.yaml` path override is still canonical (dart-lang/pub#4387; flutter.dev using-packages); Pub *workspaces* only fit a single monorepo, not the three-repo model.
Applied surgical updates to `docs/commons-migration-agent-instructions.md` (see deliverable report): justified third repo, raised Flutter floor to ≥3.24/Dart ≥3.6, reframed Phase 1 "already in base?" (SKIP-if-identical is wrong for the new dedup model), simplified Phase 3 history preservation to clean-import-default + subtree optional, made Phase 2 also confirm base participation + first slice, moved tests into Phase 4/7, made Phase 6 .gitignore mandatory + sibling-reality fix, explicited Phase 7 base-duplication conditional, and added commons to the Phase 8 test gate.
- **Flagged for user**: base repo participation (forkable must adopt commons + delete its divergent old copies), and whether to actually execute Phase 1+ soon.

## User decisions (2026-08-01, answers to grill flags)
1. **forkable WILL adopt `commons`** and delete its divergent unused copies (Phase 7 base conditional = YES).
2. **Execute the migration phase-by-phase**: summarize/grill before each phase, user review after each phase, one phase at a time with explicit permission to continue.
3. **`commons` lives at `/Users/rubenk/projects/commons`** — outside khel-service, sibling of forkable; khelam therefore needs the ABSOLUTE path override in Phase 6 (doc's input block + sibling-layout note updated to match).

## Commons migration — execution (Phases 0–8)
- **Phase 0**: scaffolded `~/projects/commons` (`flutter create --template=package`, `fde167d chore: scaffold commons package`); remote created by user in UI → `github.com/rubenkaksh/commons` (**public**); pushed `main`.
- **Phase 1**: scan → `docs/commons-candidates.md` (untracked): Slice 1 = 9 `lib/ui/common/` widgets (all coupling-clean; 5 NEW, 3 dedup-identical to forkable, 1 divergent `feedback` — child's `!`-free version canonical); Slice 2 = `dio_api_client` (divergent, child extended); Slice 3 = theme (4 files); Slice 4 = navigation (`app_router` COUPLED, `app_routes` divergent).
- **Phase 2 (approval)**: user approved manifest + recommendations; decisions: `app_router.dart` and `app_routes.dart` **stay in child**.
- **Phase 3–4**: extracted 9 widgets + 2 tests (`bottom_sheet`, `phone_input` — 15 tests) into commons; restructured `lib/src/widgets/` + barrel `lib/commons.dart`; removed template boilerplate; README documents the material name-collision convention (`FilledButton`/`IconButton`/`OutlineButton`/`Title` shadow material — consumers prefix material `as m` or `hide`). `flutter analyze` clean, **15/15 tests pass**.
- **Phase 5**: `version: 0.1.0`, commit `a8cb45a feat: initial commons package v0.1.0`, tag `v0.1.0`, pushed.
- **Phase 6**: both consumers wired — `pubspec.yaml` git dep `commons @ v0.1.0` (committed), `pubspec_overrides.yaml` (ignored; khelam = absolute `/Users/rubenk/projects/commons`, forkable = relative `../commons`), `.gitignore` entries added. Both `flutter pub get` resolve via override.
- **Phase 7**: khelam deleted `lib/ui/common/` + `test/ui/common/`; 4 consumers rewired to `package:commons/commons.dart` (no `hide` needed — all use `material as m`); `docs/commons.md` roster updated to point into commons. forkable deleted its 4 unused widget copies (`buttons`, `feedback`, `inputs`, `typography`); its `data/services/*` + `dio_api_client` are STILL USED by forkable's pre-ADR-0004 DI → kept for Slice 2/forkable cleanup. Tests: khelam **36/36** (51 − 15 moved), forkable **13/13**, commons **15/15**.
- **Phase 8**: pub get ✓ all three; analyze clean all three; tests pass all three; git-dep fallback verified (override removed → `commons 0.1.0 from git … at a8cb45`); `flutter build apk --debug` **built** ✓; `pubspec_overrides.yaml` ignored in both consumers ✓.
- Uncommitted: khelam (pubspec.yaml, pubspec.lock, .gitignore, deletions of lib/ui/common + test/ui/common, docs/commons.md, docs/commons-candidates.md untracked); forkable (pubspec.yaml, pubspec.lock, .gitignore, 4 widget deletions — plus forkable's own pre-existing uncommitted changes, untouched).
- **Committed**: khelam `adeec61 refactor: consume commons package for shared widgets` (incl. docs/commons.md roster update, docs/commons-candidates.md, migration agent instructions); forkable `b1d82de refactor: adopt commons package, drop local widget copies` (only migration files; forkable's own DI work-in-progress left uncommitted).
- **Slice 2 (DioApiClient → commons)**: commons v0.2.0 (`6378882 feat: add DioApiClient (v0.2.0)` + tag) — `lib/src/network/dio_api_client.dart` (khelam's extended copy canonical; forkable's old copy lacked getJsonList), barrel export, `dio: ^5.9.2` dep, 6 tests moved (commons now 21/21). khelam: 4 consumers rewired to barrel, `lib/core/network/dio_api_client.dart` + test deleted, ref v0.2.0 → **30/30** tests. forkable: `data_dependencies.dart` rewired to barrel, old `lib/data/services/dio_api_client.dart` deleted, its own 2 dio tests kept and now run against commons, ref v0.2.0 → **13/13** tests. Both consumers resolve v0.2.0 via local override; git-dep fallback intact.
- **Slice 3 (theme infra → commons)**: librarian research → Option A split move approved (identity stays in project). commons v0.3.0 (`415e414 feat: add shared theme builders (v0.3.0)` + tag) — `lib/src/theme/component_themes.dart` (`ComponentThemes.build()` palette-agnostic, `borderRadius`/`buttonShape` params, `lightColorScheme()`/`darkColorScheme()` REMOVED — projects own ColorScheme), monolith split into 9 group files (`lib/src/theme/groups/`: button, navigation, surface, input, selection, list, menu, picker, misc), `typography.dart` (AppTypography) moved as-is, barrel exports. khelam + forkable: `app_theme.dart` rewritten — imports commons, builds own `ColorScheme.fromSeed` from local `AppPalette`, keeps `theme/app_palette.dart`; deleted `theme/app_component_themes.dart` + `theme/app_typography.dart`; ref v0.3.0. All green: commons 21/21, khelam 30/30, forkable 13/13. Theme monolith watchpoint cleared (was 591-line file).

## Decisions (migration)
- **Clean single-commit import** into commons (no history transfer) — 9 small files, child history stays in child.
- **Slice 1 only** in this pass; DioApiClient (Slice 2), theme (Slice 3), navigation (Slice 4) deferred; forkable's BASE-LEGACY services + old DI = follow-up cleanup.
- **Name collisions**: commons deliberately shadows material classes; codebase convention = `material as m`.
- **`docs/commons.md` remains the roster manifest** for the commons package (not khelam-specific anymore).

## Blockers
- (carried) None operational. New: nothing — base repo is co-located and reachable, but the three-way-sibling path-override requirement is structurally broken for this machine layout (flagged in doc update).
