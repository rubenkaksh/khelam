# Session — 2026-08-15 (khelam)

## Objective
C20 khelam side: real turfs from API (card selection screen), booking payload
customer info, schedule flow regression fix, end-to-end verification.

## Work Log
- **C20 implementation (direct, PR #5)**: branch `feat/c20-real-turfs-e2e`.
  - TurfSummary model: +`coverImageUrl`/`pricePerHour`/`advanceAmount`/`rating`
    with `@JsonKey` snake_case mapping; build_runner regenerated.
  - TurfsApiRepository: dummy 2-turf fallback removed → real `GET /turfs`, errors
    propagate via mapDioException.
  - BookingApiService.getTurf(): real `GET /turfs/:id` (was hardcoded dummy).
  - customerPhone threaded end-to-end (confirmation sheet → cubit → API body).
  - turf_selection_view: dropdown → card list (name, address, ₹/hr, rating,
    Image.network w/ placeholder fallback, selection highlight).
  - turf_header: +price/rating.
  - 4 test files updated; `flutter analyze` clean; full suite 99/99.
  - Commits 18b142b + e42d897 (missed-file fix). Draft PR #5.
- **Schedule-flow regression + revert** (commit `67e518e`): the real `getTurf()`
  made the schedule screen hit `GET /turfs/:id` BEFORE `GET /slots?turfId=` —
  if the turf call fails, slots never load. User decision: schedule stays
  **slots-only, one request**. Reverted getTurf() to the no-network local dummy;
  test now asserts no network call. 99/99 green.
- **Booking 500 fixed**: `POST /slots/:id/book` → 500. Backend log:
  `P2022 — column "customer_name" of relation "bookings" does not exist` — the
  migration existed but was never applied. Applied via `npm run migration:up`
  (batch 3). Columns verified in schema + migration:status.

## Decisions
- Schedule screen = slots-only (no /turfs/:id round-trip) — user decision.
- Booking sends customerName + customerPhone; backend persists as
  customer_name/customer_phone.

## Environment
- Backend: `npm run start:debug` on :8000 (inspector :9229) — see backend session.
- API_BASE_URL=http://localhost:8000 active in .env.

## Blockers / Next Steps
- Review PR #5. Turf card images still placeholders (no real cover URLs seeded).

## Final Handoff (session end 2026-08-15)
- **C20 nearly complete** — PR #5 (`feat/c20-real-turfs-e2e`) carries: TurfSummary
  fields + card selection screen, real `/turfs` list (no dummy fallback), customer
  name+phone in booking payload, schedule stays slots-only (getTurf reverted to dummy
  per user decision).
- **bookedBy/bookedByContact + tap-to-call** (commit `fc36c97`): ScheduleSlotItem
  +`bookedByContact`; BookedSlotCard tap → `tel:` call when contact present
  (`url_launcher` added). 55/55 booking tests, analyze clean.
- **Backend dependency**: booking 500 fixed by applying migration
  `20260814120000_add-booking-customer-fields` (batch 3) — was P2022 ColumnNotFound.
- **Environment**: backend running (`npm run start:debug`, :8000); `.env`
  `API_BASE_URL=http://localhost:8000` active.
- **Next session**: review + merge PR #5; verify tap-to-call on device; seed turf
  cover images when real ones exist.
