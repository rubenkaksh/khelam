# Session — 2026-08-08

## Objective

`Feature: n/a — Robustness batch T2 → T3 → T5 (commons-first, per 08-07 handoff + tasklog active queue). After: theme/real-user-flow.`

## Context carried forward (from 2026-08-07 session + handoff)

- **Active queue (locked, user 08-07)**: T2 shared error mapping → T3 retry/connectivity → T5 in-session GET cache. All commons-first. T4/T6 DESCOPED (parked, revisit 08-16). macOS DMG blank-screen issue parked (08-16).
- **T-tasklist detail**: `docs/backlog.md` "From the 2026-08-07 startup-architecture audit (T-tasklist)"; audit report `~/projects/sandbox/tmp/startup-architecture-audit-2026-08-07.md`.
- **T2 spec**: `AppException` hierarchy + `DioApiClient` 4xx/5xx/timeout mapping; drop `catch (_)` discards (`schedule_cubit.dart:95,121,156` + `turf_selection_cubit.dart:78`); `EmptyView` for zero slots (`schedule_view.dart:71-130`). Auth slice proves pattern.
- **T3**: commons `RetryInterceptor` (1–2 attempts, backoff) + connectivity gate (not single-timeout hard-fail); fix stale-turf masking fallback in `TurfsApiRepository`.
- **T5**: commons `DioApiClient` GET cache (~30s TTL) for date re-selects.
- **commons-first mechanics**: bump commons `0.6.0 → 0.7.0` + tag; khelam consumes commons (path override per pubspec_overrides per handoff). Consumer check: analyze commons + khelam + forkable after each commons change.
- **Task board**: `docs/tasklog.md` is the LIVE board — open first, update as cards move, commit with day's work. Statuses 🔴/🟡/🟠/⚪/⚠️/📋.
- **OA#8 bookkeeping**: Discord v2 impl shipped 08-07 (@general batch: report_sink v2 + daily_digest.sh + plist template + channel routing) but review-memory still shows OPEN → close with go-live parked in tasklog.
- **Environment**: backend :8000 (node PID 9903, NEVER kill, log /tmp/rms-backend.log); iOS sim `4509FAFA` + Android emulator-5554 booted; khelam main clean; `.env` untracked+gitignored (user's uncommitted Render switch — never commit); commons idToken debug instrumentation dirty (never stage).
- **Delegation cadence**: no long subagents in interactive session without user OK first (08-07 queuing discussion). Standing directive: agent-tooling/review work → subagents.
- Pre-commit gate runs analyze + full suite per Dart commit; docs-only commits skip.

## Work Log

- **Session start**: read session 08-07, review-memory (OA#8 still marked OPEN — impl shipped 08-07; bookkeeping to close), handoff, tasklog (T2 NEXT). Environment verified: backend :8000 (PID 9903) alive, khelam clean (only new session file), commons dirty (graphify-out + idToken instrumentation + .vscode — never stage), forkable dirty (GeneratedPluginRegistrant + pub-additions.md — leave). Created this session file (Hard Rule #1).
- COST-NOTE (est ~15–20k): T2 (commons mapper + tests, khelam cubits + EmptyView + tests, 3 analyze runs + gate).
- **T2 SHARED ERROR MAPPING — DONE 2026-08-08** (codegraph lookups: `DioApiClient` @ commons lib/src/network/dio_api_client.dart:3 (members raw/setBearerToken/getJson/getJsonList/postJson/_expectObject, callers: test/dio_api_client_test.dart); `AuthApiService._mapDioError` @ auth_api_service.dart:147 (the proven mapping pattern); `ScheduleCubit` load:69/selectDate:106/bookSlot:132 (TurfSelectionCubit NOT in codegraph index — direct read, noted)). Commons: new `lib/src/network/app_exception.dart` — sealed `AppException` hierarchy (client/server/timeout/offline/unexpected, `toString() => message`), exported via commons.dart; `DioApiClient.mapDioException` (4xx → AppClientException preferring server `message` field, 5xx → AppServerException, timeouts → AppTimeoutException, connectionError → AppOfflineException, else AppUnexpectedException); `getJsonList` blanket catch REMOVED (was hiding the dio error type — the root of TurfsApiRepository's 404-masking confusion). **DESIGN DECISION A→B (gate failure caught it)**: first cut auto-converted DioException→AppException at the DioApiClient boundary; pre-commit gate exposed 5 auth_api_service_test failures — auth's `_exchange` catches `on DioException`, so the boundary conversion silently bypassed auth's flow-specific mapping (violates audit's "auth stays as-is"). REVERTED to design B: mapper is public, services map explicitly in their own `on DioException` catches (audit's stated design). Commons `6976014` + revert `827f940`, tag **v0.7.0** re-pointed to `827f940` (tag was pushed at the wrong commit — force-moved before any consumer pinned it; khelam uses path, forkable pins v0.6.0). Khelam `b1ecd81`: ScheduleCubit/TurfSelectionCubit `on AppException catch (e)` → surface `e.message` (generic fallback for non-AppException), BookingApiService maps via `mapDioException` at the service boundary (auth untouched), ScheduleView EmptyView branch ("No slots available for this date."; `turf != null` guard prevents pre-load flash). Tests: commons dio_api_client 12/12 (mapper-direct, incl. server-message preference, 401-without-message, 503, timeout/offline/unknown; `getJson rethrows DioException on non-2xx` restored), khelam cubit typed-surfacing ×3, booking service 503/offline mapping, new `test/features/booking/widgets/schedule_view_test.dart` (EmptyView via GoRouter + `_EmptySlotsBookingService`), existing 404-fallback regression green. Gate passed (84/84), pushed both repos, consumer analyze clean ×3.
- **T3 RETRY + CONNECTIVITY-AWARE — DONE 2026-08-08** (codegraph lookups: `TurfsApiRepository` NOT in codegraph index — direct read turfs_api_repository.dart + turfs_api_repository_test.dart + service_locator.dart:32-43, logged; commons RetryInterceptor: new symbol, codegraph `graphify query` n/a pre-edit — new-file carve-out, noted). Commons: new `lib/src/network/retry_interceptor.dart` — `RetryInterceptor` (Dio `Interceptor`): retries **GET-only** (POSTs never — a retry could double-book a slot), maxAttempts 2 (original + 1 retry), linear backoff `baseDelay * attempt` (300ms base), retryable = connectionTimeout/receiveTimeout/sendTimeout/connectionError/HTTP ≥500; attempt count tracked in `options.extra['retry_interceptor_attempt']`; re-issues via `_dio.fetch` so the full chain (incl. itself) runs; exported via commons.dart. Commons commit `eb05ff6`, version bump `203af79` → **v0.7.1** (tag v0.7.1 pushed; forkable still pins v0.6.0 — unaffected). Tests `test/retry_interceptor_test.dart` 5/5 via `_CountingAdapter` (failures-before-success): 503→retry-success (2 calls), exhaustion (2 calls), connectionError→retry-success, 4xx NOT retried (1 call), POST NOT retried (1 call). Khelam `d513184`: `TurfsApiRepository.getTurfs` — blanket `on Exception` dummy-turf fallback REPLACED with 404-only (`on DioException` → `mapDioException` → only `AppClientException` statusCode 404 serves the 2 known turfs; offline/timeout/5xx propagate typed so the user sees the real message instead of stale dummies; doc-comment + TODO-until-endpoint note updated); `service_locator.dart` DioApiClient lazySingleton now also adds `RetryInterceptor(dio: client.raw, baseDelay: 300ms)` AFTER samseer (added last so its onError runs first; comment notes POSTs never retried). Tests: turfs_api_repository_test — stale `_NotFoundAdapter` replaced with `_ThrowingAdapter.failure({statusCode|type})`, 404-fallback test comment updated + 2 NEW propagation tests (connectionError → `AppOfflineException`, 500 → `AppServerException` — the masking bug is now pinned by test). Full suites: commons 50/50, khelam 93/93, forkable analyze clean (pins v0.6.0). Pushed both repos; session COST-NOTE merged below.
- COST-NOTE (est ~10–12k; actual ~13k): T3 (commons interceptor + tests + release commit, khelam repo fix + DI + tests, 3 full-suite runs + gate + forkable analyze).
- **T5 IN-SESSION GET CACHE — DONE 2026-08-08** (codegraph lookups: `DioApiClient` @ commons lib/src/network/dio_api_client.dart:5 (members raw/setBearerToken/getJson/getJsonList/postJson/mapDioException/_expectObject, callers: test/dio_api_client_test.dart + khelam booking_api_service.dart:47 + turfs_api_repository.dart:27); read-only check of both callers (direct reads, noted). `DioApiClient` gains an in-memory GET cache: constructor param `getCacheTtl` (default 30s), keyed by path + **sorted query params** (`_key`) — the `turfId`+`date` re-select pair gets its own entry per date, so re-selecting a date is served from memory while a different date re-fetches. Only **successful** responses are cached (never errors — `_cachePut` runs after a 2xx; `_cachedGet` evicts expired entries on read; `_pruneExpired` keeps the map bounded to live entries). **Invalidation**: successful `postJson` clears the whole GET cache (booking mutates slot availability — the just-booked slot must not stay invisible behind a cached pre-booking response for the TTL) and `setBearerToken` clears it (account boundary — a different user never sees the previous user's data). Shape checks (`_expectObject`/`_expectObjectList`) run on cache hits too, so behavior is identical hit-or-miss. Commons commit `a8f1211`, version bump → **v0.7.2** (tag pushed; forkable still pins v0.6.0). Tests: `_CountingJsonAdapter` (call-counting + per-request `body` factory) — 7 new tests: repeated getJsonList within TTL → 1 network call; different date → 2 calls; getJson object cached too; `getCacheTtl: Duration.zero` → always re-fetch (deterministic expiry, no sleeps); failure not cached (503 then success → 2 calls); postJson flush (GET+POST+GET → 3 calls); setBearerToken flush → 2 calls. dio_api_client_test 19/19. **khelam: NO code change needed** — cache is transparent to consumers (methods return the same shapes; a 404 still rethrows DioException from the non-cached path since failures never cache). Verified: commons full suite 57/57, khelam 93/93 (unchanged), forkable analyze clean. Pushed; robustness batch T2→T3→T5 complete. Session COST-NOTE merged below.
- COST-NOTE (est ~8–10k; actual ~12k): T5 (commons cache + 7 tests + release commit, 3 full-suite/analyze runs, 2 test-fix loops on adapter shape + call count).
- **AUTO-DIGEST RELIABILITY DESIGN — APPROVED (2026-08-08)** (no codegraph lookups needed — bash scripts, not Dart; direct reads of daily_digest.sh/report_sink.sh/plists + `man launchd.plist` verification, logged). User-initiated brainstorm ("nothing hinders the auto-digest"): verified every dependency (all tools resolve in plist PATH; python3 is the only hard dep; opencode.db 665 MB WAL-mode at ~/.local/share/opencode/; job never fired via launchd — /tmp/daily-digest.log absent; both installed plists current w/ REPORT_SINK). Answers: **agent does NOT need to run** (script is self-contained shell; the agent only produces data); **laptop-on mostly enough** — sleep is handled by launchd wake-coalescing, but power-off/logout/login-screen at 08:00 = missed day. Locked requirements (user): catch-up + retry REQUIRED (daily digest is the user's MOST important automation — decisions/tracking; weekly = broad perf/cost, deliberately untouched); **one digest per missed day, oldest-first**; duplicate prevention mandatory; v2 direction = webhook → agent activation (foundation only, no v2 design). **@architect delegated** (task ses_01fd12115ffeXb4XVupPg1ImwO): verified StartInterval fires relative-to-load (killed poller approach B), RunAtLoad+calendar coexist, sqlite WAL read-safe; caught the **Friday gap** — normal Mon–Fri fires report Sun–Thu only (Fri's fire reports Thu; Sat no fire; Mon reports Sun) → due-set needs a Friday exception. **Recommended + approved: approach A refined** — plist `RunAtLoad=true` + calendar stays; script refactor to `generate_body(D)`/`is_due(D)`/`send_with_retry`/`main()`; persistent markers `~/Library/Application Support/khelam/daily-digest/markers/YYYY-MM-DD.sent` (written post-send only, NOT /tmp, NOT repo); atomic `mkdir` lock vs wake-fire/login-fire race; 3-attempt backoff retry (0/5/15s, ~65s worst case) + agent-errors report; 14-day lookback oldest-first; LOG moves to persistent `$STATE_DIR/daily-digest.log`; guard skips silent. Failure contract: never-lose, ≤1 dup (marker-write-after-post edge, accepted over pre-marker data-loss window). Spec: `docs/superpowers/specs/2026-08-08-auto-digest-reliability-design.md` (approved, awaiting implementation). Implementation is forkable-first (canonical scripts) → sync khelam → installed plist + reload → idempotency/catch-up/kickstart E2E tests.

## Decisions

- **Google sign-in DESCOPED (user 2026-08-08)** — google_sign_in had issues; low priority. `google_sign_in` (auth feature) + **Action #1 (rotate leaked Google SERVER client ID `91250679358-frejgd…`)** both park until the user decides to pick up Google login. Board: tasklog ⚪ Descoped. **REMINDER FOR LATER:** the server client ID is still in git history + live `.env` — rotation runbook stands ready (console → new web-app OAuth client → update `.env` GOOGLE_SERVER_CLIENT_ID → check backend `aud` → delete old → optional filter-repo). Reopen = user decides to revisit Google login.
- **Discord env sample created (2026-08-08)** — `~/.config/opencode/discord.env` with pre-written channel keys (`DISCORD_WEBHOOK_URL` alias + `DAILY_OVERVIEW/WEEKLY_REVIEWS/SCREENSHOTS/AGENT_ERRORS_WEBHOOK_URL`), URLs empty (safe: empty URL → macos_notification fallback per report_sink.sh:55/127). User fills URLs in-editor; agent never echoes them in chat. REPORT_SINK=discord_webhook still needed in both plists (not yet set); digest plist not installed.
- **Discord go-live progress (2026-08-08)** — user filled all 5 URLs in `~/.config/opencode/discord.env` (verified non-printing: 5 filled / 0 empty); mode tightened 644 → 600. `REPORT_SINK=discord_webhook` added to both plist templates (forkable `acbbf04` on test/aider — its de facto branch, origin/main stale at initial commit; synced to khelam `d072c6c`), both lint-clean. Remaining user steps: update installed weekly plist (predates template change), install digest plist, E2E `daily_digest.sh` → expect #daily-overview post.
- **Discord go-live COMPLETE + webhook E2E (2026-08-08)** — user installed/updated both plists + ran digest E2E. Live test sweep via `source scripts/report_sink.sh` (bash — note: sourcing under zsh breaks `_discord_url_for`'s `${!key}` → "bad substitution"; run test harness under explicit bash): **4/5 passed first pass** (daily-overview, weekly-reviews, screenshots multipart w/ 1×1 PNG, agent-errors w/ Sosumi); **default FAILED** — log: `curl: (1) Protocol "ttps" not supported` → `DISCORD_WEBHOOK_URL` missing leading `h` (user fixed). Retest: default ✅. Also posted real screenshot `schedule_20260807_132333.png` (257 KB, 1320×2868) to #screenshots — visible confirmation; the earlier 1×1 PNG was stored but invisible (width:1). **All 5 webhooks verified.** Board: go-live closed 📋; parked "Discord go-live steps" card + ⚠️ user actions section removed (empty). First auto-digest Mon 08-10 08:00.
