# Weekly Review — 2026-08-10

> Window: sessions 08-05 (tail) → 08-10 (47 rows in `~/analytics/weekly/2026-08-09.csv`, 306.5M tokens, $0). Runs Monday because the scheduled Sunday 18:00 run failed silently — see Waste #1. Mechanical checks: tooling tripwire OK (4 sessions log codegraph/graphify vs 9 grep/read); index freshness FAILED (khelam + forkable codegraph pendingChanges=1); session-boundary FAILED ("List all skills" 140M cache / 129.1h); forkable-sync DRIFT (bootstrap.sh); config-guard OK.

## What shipped this week

- **Robustness batch T2→T3→T5 (commons-first)** — commons v0.7.0→v0.7.2 (`6976014`/`827f940`, `eb05ff6`/`203af79`, `a8f1211`): sealed `AppException` hierarchy + `mapDioException`, GET-only `RetryInterceptor` (never re-POSTs a booking), in-memory GET cache with postJson+logout invalidation. khelam: typed error surfacing + EmptyView (`b1ecd81`), 404-only dummy-turf fallback + interceptor wiring (`d513184`).
- **Turf-selection screen + shared Preferences store** — `/select-turf` initial route, `ScheduleCubit` turfId param, logout clears prefs (`f546a0c`, spec `f721c08`).
- **Discord comms v2 + daily digest** (Grill Gate 12/12) — multi-channel `report_sink.sh`, `daily_digest.sh`, plist template; all 5 webhooks live-verified 08-08 (`e0f59bf`, `8cab2a4`, `fedabea`, `41d2ed7`); digest = single trimmed board message, no .md attachment.
- **Auto-digest reliability** — catch-up + retry, per-message markers, stale-lock takeover (`7f52e4b`, `eea5e4d`); **first scheduled fire Mon 08-10 08:06 succeeded** (1 post, 0 dupes, marker `2026-08-09-1.sent`) → OA#9 CLOSED.
- **Governance forkable-first + sandbox v2** (`e54252d`, `0fbef0d`, `f1a54e0`) — 18-denies global guard, .env deny in grants, `config_guard_check`, deny-deferral log, test-window revoke 08-21.
- **Auto index refresh (hook-only)** (`0e7ad79`), **melos batch runner + developer-tooling.md** (`6a3bf98`), **theme-config light palette** (`4346f55`), **+91→+977 commons-first** (`f079a70`), **.env untracked (T1)** (`9445e27`), task dashboard dropped user-signed (`ec8baf0`).
- **Weekly-review failure-silence fix + retry leg + Discord delivery v2** (summary + chart attachments) — forkable-first, **uncommitted** in both repos (see Waste #1).

## Waste observed

1. **Silent 08-09 18:00 review failure (top)** — `flutter analyze` inside `$(...)` under `set -e` + pipefail; flutter not on launchd PATH → exit 127, zero notification. Cost: a full missed review + re-run today (~3M tokens incl. the 08:46 auto-run). Fixed (SIGPIPE guard, flutter guard, EXIT trap, 3-attempt retry, Sun 19–22h retry leg) — **but the fix is uncommitted working-tree state in both repos, and `bootstrap.sh` was not re-pulled → forkable-sync DRIFT**. Avoid: commit forkable-first + pull byte-identical; retry leg covers future failures.
2. **Long-session cache burn** — "List all skills" 151.1M tokens (140.8M cache, 129.1h) = **49% of the week**; 08-07/08 continuation sessions 41.8M + 36.1M; 08-09 "Daily digest not received" 13.8M (18.9h span). Fresh-session-per-batch still not followed for interactive days.
3. **@librarian EMPTY returns ×2 (08-07)** — 0-token returns forced ~2.1M-token general-agent re-runs (ecosystem research + permission semantics); the ecosystem report was delivered twice. 4th/5th documented occurrence.
4. **Digest format churn (08-08)** — 3 layouts in one day (counts → 4-message → single trimmed), 2 live E2E cycles + 12→48 marker migration; final layout spec arrived after implementation.
5. **Sandbox scope correction** — agent implemented the `~/projects/**` boundary without asking the directory-scope question; user corrected → recovery brainstorm + research ~18k. Agent-side scoping failure (rule exists).
6. **"3 issues" stale premise** — 3 analyze runs on a stale IDE LSP artifact (zero code issues); verify-before-debug worked, cost trivial.
7. **@architect subagent overwrote `docs/sessions/2026-08-07.md`** (shared state) — restored from HEAD, lost entries re-applied; mitigation: "do not touch docs/sessions/" in subagent briefs.

## Top 3 cuts for next week

1. **Commit the review-machinery fix batch forkable-first** (weekly_review.sh, retry script/plist, bootstrap.sh, review_chart.py, spec amendment) + pull khelam byte-identical → closes the forkable-sync DRIFT **and** the codegraph pending=1 freshness FAIL (hooks re-sync on commit). User sign-off required; all uncommitted.
2. **Enforce session boundaries for real** — one 129h session = 49% of the week's tokens; end interactive sessions at task boundaries; keep the >1d/>50M mechanical flag (also watch 6–7h/35M days).
3. **Stop double-research + redesign churn** — delegation briefs must demand a returned summary (librarian empty = failure → retry scout or use @general); full layout specs (digest) before implementation.

## One thing that went well

Open Action discipline + tooling flip: all 9 prior OAs resolved with user sign-off (#1–#6 closed 08-07, #7 dropped 08-08, #8 closed, #9 verified live on first fire); codegraph/graphify usage went 0 → ~18–20 logged lookups; every major-drift trigger went through a full architect grill with user answers (Discord v2 12/12, auto-digest, auto-index, sandbox C.1–C.6, theme brainstorming); the digest's first scheduled fire landed exactly as designed (1 trimmed message, no dupes).

## Memory update (changes to make in docs/reviews/review-memory.md)

- Review History: append row 2026-08-10 (7 waste incidents; silent review failure fixed-uncommitted; long-session cache = top lever at 49%; OA#9 closed on first fire).
- Open Actions: **#9 → CLOSED** (first scheduled fire verified 08-10 08:06; user walkthrough conversational, not blocking). **Add**: (a) commit + re-pull the review-machinery batch (forkable-sync DRIFT on `bootstrap.sh`; also resolves codegraph pending=1 in khelam + forkable); (b) @librarian empty-return mitigation (delegation briefs demand a returned summary; prefer @general for web research); (c) booking-calendar README refresh — ADR routing row stale (`/select-turf` initial route since `f546a0c`; robustness layer unreported); (d) Google SERVER client-ID rotation needs a date (undated, security-relevant).
- Sandbox/guard: CONFIG-GUARD OK (no `external_directory` added to any grant — verified 0/4); deny-deferred 08-07 `~/.ssh/config` = deliberate battery test, auto-denied, no retry — no violation.
- Grill Gate: Discord v2 (12/12), auto-digest, auto-index-refresh, sandbox v2 (C.1–C.6), theme — all passed @architect + user sign-off; none skipped.

## Performance Summary

See `/Users/rubenk/analytics/performance-summary.md` — 306.5M tokens ($0), 47 sessions, mean estimate error ~43% (15/19 within or under; worst Discord v2 2×), 7 waste incidents, ~18–20 codegraph/graphify lookups, ~17–19 full test runs (gate-mandated) + 3 justified integration runs.

## Feature Audit

One declared feature: `docs/features/booking-calendar/README.md` (97 lines). Implementation checklist 15/15 `[x]` (100%), Progress Tracker 3/3, ADR table refreshed 08-07 (OA#4). No new features declared this week (theme + robustness batch tracked in tasklog). **Stale flag**: ADR Navigation + DI/Routing rows still say `/schedule` initial route — app boots to `/select-turf` (`f546a0c`), `/schedule` needs a `turfId` param; turf-selection + EmptyView + commons v0.7.2 layer not reflected → new Open Action (c).
