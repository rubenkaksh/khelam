# Weekly Review — 2026-08-06

## What shipped this week
- Booking API data layer (`0ad5790`, `5c6aef9`, `ccb38c0`): `/slots` + `/slots/:id/book` behind `BookingApiService`, parsed straight into the existing freezed domain models.
- ADR-0004 architecture refactor (`601ec7b`): features own their vertical slice under `lib/features/`, pass-through repositories deleted, `CONTEXT.md` written.
- commons v0.1→v0.6 consumed (widgets `adeec61`, Dio `4d38395`, theme `0d33fa1`, login contracts `c94c7d9`, GoogleSignInService `46b57bf`); codegraph→graphify→both (`ad895a1`, `7817656`); commons graph built, cross-repo merge-graphs live.
- Google sign-in to `/auth/google` (`2d15eaf` + slices), phone login/register (`8dc074e`, `dd146a2`), registration UI + public `/register` (`e5aa9a6`).
- Booking guard (`0544514`) + logout (`0936c03`); live E2E booking verified (`61ed25f`) with a non-destructive slots ALTER; samseer HTTP inspector wired in khelam (`642d760`) and forkable (`3d9ac70`).
- Cost infrastructure: `weekly_review.sh` + launchd (`145d653`), pre-commit gate + shared test fake (`3e455a8`), review-memory.md (`2e6ebc2`), global sidetrack guard (`5c3a8d8`).
- v2 analytics pipeline: ccusage collector + charts + report_sink, weekly_review.sh v2 (Sections A/B/C), booking-calendar feature README (`cf78fd9`, `bb05fc9`, `559b605`).
- Background-agent v1 experiment: batches 1–5 done (AGENTS.md hierarchy, analytics, review v2, feature docs), scope=clean throughout.

## Waste observed
- **One 5-day session carried the week** (new — analytics-driven): `ses_0440d96e` "List all skills" — 126M cache reads, 8.2M input, 1,530 messages, alive 08-01→08-06; 75% of the week's 167.5M tokens. The fresh-session-per-batch model (08-05 spec) was never enforced; even the 08-06 experiment ran inside it. Avoid: hard session boundaries, compact/archive long sessions.
- **Tooling discipline, second week running**: the mechanical tripwire passes only because 08-01 logged graphify query/path/explain; feature-work sessions (07-31, 08-03, 08-04, 08-05, 08-06) logged ZERO codegraph/graphify lookups — the pre-edit rule (08-05) still shows no visible lookup lines. Avoid: tripwire must count work-session lookups, not tooling-setup sessions.
- **Open Action #1 still open**: `registerLabel` getter missing in commons `test/auth/login_screen_test.dart` `_FakeStrings` (line 6); discovered 08-06 B2.4, deferred to this review, still unfixed (verified). Commons' own analyze was red all week over a 1-line test fake.
- **Commons origin break unchecked**: `1537aa1` broke forkable's compile (caught + fixed in-session, `d75b0f1`) but commons itself stayed broken — the consumer check checks consumers, not the origin.
- **Design-cost spike**: architect session `ses_02e614eb` = 317k input tokens for the v2 architecture design; high value (became the executed handoff) but the week's biggest subagent cost — future design grills need cost-notes.
- Minor / re-evidenced: 9 khelam full test runs (2–3 insurance-level; the pre-commit gate now covers this); double @librarian PDF invoke (2 sessions, ~1.4k/0.8k output — see 08-05 review); 08-04's 3 API tests deferred (root cause was a 1-line nullability fix, `dd146a2`).

## Top 3 cuts for next week
1. **Enforce session boundaries mechanically**: fresh session per batch; the weekly check should flag any session older than 1 day with >50M cache reads. The 08-05 spec already requires it — the 126M-cache session proves intent alone isn't enough.
2. **Make the tripwire count real work-session lookups**: exclude tooling-setup sessions (the 08-01 class), and require the pre-edit rule's lookup line to appear in the Work Log for every symbol edit — no line, review flags it.
3. **Close registerLabel + check the origin**: add the 1-line getter in commons (closes Open Action #1), and extend the session-time commons check to analyze commons itself, not just khelam/forkable.

## One thing that went well
Live booking E2E (`61ed25f`): the last unverified piece of the booking feature — real JWT, real backend, and a non-destructive ALTER for the slots drift — captured as a permanent integration test instead of a one-off curl.

## Memory update
- `docs/reviews/review-memory.md`: append 2026-08-06 row to Review History (session-boundary waste, tooling tripwire gap, registerLabel).
- Actions: KEEP #1 (registerLabel, with explicit 1-line fix); ADD #2 session-boundary enforcement, #3 commons self-analyze, #4 refresh booking-calendar README Out-of-scope list.
- Measures table: add v2 analytics pipeline (collector/charts/report_sink/Sections A/B/C), Objective convention, feature README migration.

## Performance Summary
See /Users/rubenk/analytics/performance-summary.md — 167.5M tokens (77.7% from the long-lived session), $0.00 cost (free-tier models), 7 waste incidents, estimate accuracy ~59% (safe direction, 0 overruns), 9 khelam full test runs, ~6 logged graphify/codegraph lookups (all in 08-01), 23 sessions. First week of v2 data — trend column "—" everywhere.

## Feature Audit
booking-calendar: README migrated 08-06 (`559b605`). Implementation checklist 100% (13/13), Test Plan green (12 tests). ADRs added: 0 this week (ADR-0004 predates the README; decisions table embedded). Scope drift: README lists "real API integration" and "auth guard on `/schedule`" as Out-of-scope, but both shipped this week (07-31, 08-05) — Out-list is stale, refresh recommended. Stale (>7 days no progress): no. No session used the `Feature:` Objective convention yet (added 08-06; `feature_parent` is "-" in all 23 CSV rows).
