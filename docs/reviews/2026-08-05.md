# Weekly Review — 2026-08-05

## What shipped this week
- Booking API data layer: API JSON parsed directly into freezed domain models (`0ad5790`, `ccb38c0`), `BookingApiService` + mock/API DI switch (`5c6aef9`).
- ADR-0004 architecture refactor (`601ec7b`): features collapsed into `lib/features/<feature>/`, pass-through repositories deleted, ADR-0004 + CONTEXT.md written.
- khelam consumed commons v0.1.0→v0.6.0 (widgets `adeec61`, DioApiClient `4d38395`, theme `0d33fa1`, LoginScreen contracts `c94c7d9`, GoogleSignInService `46b57bf`); codegraph→graphify switch (`ad895a1`) then both kept (`7817656`); commons graph built + cross-repo merge-graphs live.
- Google sign-in wired to backend `/auth/google` (`2d15eaf`, slices `da946fc`/`7d41ff2`/`10ec47e`, router fix `1a3a20e`).
- Phone login/register shipped (`8dc074e`, `76f6a66`, `51b082d`, `1249b2f`); then `AuthUser.email` → nullable (`dd146a2`) and registration UI + public `/register` route (`e5aa9a6`).
- Booking guard: unauthenticated slot tap → login → redirect back (`0544514`); logout (`0936c03`).
- Live backend E2E: auth + booking integration tests green (`9f2e9f8`, `61ed25f`); slots schema drift fixed non-destructively via targeted ALTER; samseer HTTP inspector wired in khelam (`642d760`, `7e069eb`) and forkable (`3d9ac70`).

## Waste observed
- **DTO layer built then deleted (07-31)**: a full `SlotDto`/`BookingDto` pass (models + generated + tests + service wiring) was added, then removed after the user pointed out `Slot`/`Booking` were already freezed with generated `fromJson`. One codegraph/graphify look at the existing models, or one upfront question, would have skipped the entire pass.
- **@librarian invoked twice for the knowledge-graph PDF, both returns empty**; the work was then completed directly (08-01). Double spend — verify output before re-invoking.
- **Router `isPublic` guard dropped twice**: an unrelated working-tree edit got swept into slice commit `da946fc`, fixed by `1a3a20e`; then the same condition went missing again during phone-login work and was restored 08-04. Two fix cycles for one condition — a `git status`/`git diff` check before slicing commits catches this class.
- **3 API test failures deferred 08-04** with "need DioApiClient source" — wrong lead; the root cause was khelam's own `AuthUser.email` (required `String` vs backend `email: null`), a one-line fix (`dd146a2`). Running the tests in-session and reading the stack trace beats deferring diagnosis.
- **Integration test run against a known-broken `/slots`**: the P2022 `locked_by` drift was documented since 08-03, so the first run failed predictably; the targeted ALTER and dropping the `/slots` check came after. Fix the DB (or the test) first.
- **samseer wired app-side in both repos, then user asked "move it to commons"** (backlog): future rework; an upfront "commons or app-side?" question would have avoided it.
- **Widget-test hang from unmocked FlutterSecureStorage** (08-05 evening): the `_RecordingTokenStore` pattern was documented on 08-02 and noted in the same session, yet the new test repeated the mistake (`pumpAndSettle` timeout). Extract the fake to a shared test helper and reuse.
- Minor: `dart format` churn on untouched files reverted (08-01); 08-03 debug-APK build repeated 08-02's; evening auth integration re-run covered flows already green under unit tests.

## Top 3 cuts for next week
1. **Ask one upfront question before any new layer or cross-repo wiring**: query existing models first via `codegraph explore`/`graphify query` (kills the DTO-layer class of waste), and ask "commons or app-side?" before wiring anything shared into both apps (samseer).
2. **Don't commit with known failures, and don't defer diagnosis**: 08-04 shipped 8 commits with 3 failing tests deferred; the root cause was a one-line nullability fix. Close the loop in-session — run tests, read the assertion, fix or revert before committing.
3. **Check the blocker list before E2E runs**: the `/slots` 500 was known since 08-03 — apply the non-destructive ALTER (or trim the test) before running integration, not after a predictable failure; and put the token-store fake in a shared test helper so widget tests never hit the real keychain again.

## One thing that went well
Live booking verification (08-05): `POST /slots/:id/book` exercised end-to-end against the real backend with a real JWT — the last unverified piece of the booking feature — captured as a permanent integration test (`61ed25f`), with the schema drift fixed via a targeted non-destructive ALTER instead of the destructive `prisma db push`. 66/66 unit tests, analyze clean, no `!`-rule regressions.
