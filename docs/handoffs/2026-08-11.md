# Handoff — 2026-08-11 (background loop sessions for agent-tools / loop-engineering)

## Next session focus (from user)

Run a **background session for agent-tools and loop-engineering related tasks** without the user prompting AI coding agents turn-by-turn. "All projects are settled as before."

## Where things stand (verified 2026-08-11)

- **agent-tools** at `7b7d73a` ("feat(loop): enforce loop-budget gate + skill chain in daily_triage (C4); harness for loop_verify (C2); drop VERSION (C1)") — clean tree.
- **khelam** at `260acc7` — clean. **forkable** at `e4d5c76` — clean. Both pins: `scripts/agent-tools.version` = `7b7d73a`.
- C1–C4 all DONE (tasklog rows 27–30 in `/Users/rubenk/projects/khel-service/khelam/docs/tasklog.md`). C5–C10 still `⏳ Awaiting approval`.
- Harness: **45/45 PASS** (agent-tools `tests/test_loop_verify.sh` + temp 45-test daily_triage harness). `bash -n` + shellcheck clean.
- Watch List (khelam `STATE.md` + review-memory): **OA#11** (empty subagent returns), **OA#12**, **OA#13**.
- First Sunday weekly review: **08-16** (launchd `com.khelam.weekly-review`, Sun 18:00).

## Loop infrastructure (installed, loaded, verified)

- LaunchAgents all loaded (`launchctl list`): `com.khelam.daily-triage` (Mon–Fri 07:30, real `opencode`, log `/tmp/daily-triage.log`), `com.khelam.daily-digest` (Mon–Fri 08:00), `com.khelam.weekly-review` (Sun 18:00). None have `RunAtLoad`.
- Scripts: `~/projects/agent-tools/scripts/{daily_triage,daily_digest,weekly_review,loop_verify,sync_loop_state,report_sink,pre_commit_check}.sh`
- Skills: `~/projects/agent-tools/skills/{loop-constraints,loop-budget,loop-triage}/SKILL.md`
- khelam scaffold: `STATE.md`, `LOOP.md`, `loop-budget.md`, `loop-constraints.md`, `loop-run-log.md`, `LOOP-STATE.json` (derived — never hand-edit; `sync_loop_state.sh` regenerates).

## Key facts for the next session

- **agent-tools has NO board** (no `docs/tasklog.md`, `docs/reviews/review-memory.md`, `docs/sessions/`, `STATE.md`, `loop-budget.md`). Triage skip rule 2 ("no board") will skip it unless a board exists or the run targets khelam (whose tasklog carries the loop-engineering backlog C5–C10).
- `daily_triage.sh` env knobs: `TRIAGE_REPOS` (space-sep; default DEFAULT_REPOS L83+ = khelam, forkable, commons, rms-futsal-backend, sandbox — **agent-tools not included**), `TRIAGE_OPENCODE` (default `opencode`), `TRIAGE_LOG` (default `/tmp/daily-triage.log`), `REPORT_SINK` (default `macos_notification`).
- `repo_paused` (L130–134) scans `STATE.md` + `docs/tasklog.md` for `loop-pause-all` via **chained `grep -qs` (no pipeline)** — pipefail-safe on BSD grep (grep exits 2 on missing file even with `-s`). Do NOT extend to `loop-budget.md` (it documents the label → false trip).
- Skip-rule log prefix convention: `skip $name:` (line 214, etc.).
- Budget gate (budget_status, L136+): ≥100% → skip, ≥80% → report-only. Reads `loop-budget.md` + `loop-run-log.md` (python3 run_id filter, 24h window).
- PROMPT skill chain (L243+): loop-constraints → loop-budget → loop-triage.
- **Write-layer degradation caution**: edits/commits have reported success without persisting (twice this session). After ANY write/commit, verify with `git rev-parse --short HEAD` + `git status --porcelain` before trusting it.
- Pre-existing dirt NOT from this session, leave alone: commons (`M lib/src/auth/google_sign_in_service_impl.dart`, `?? .vscode/`), rms-backend (`?? .opencode/`, `?? docs/backlog.md`, `?? docs/sandbox/`).
- Demo seam: phone `9800000001` (see khelam AGENTS.md) — do not print secrets in docs.

## Suggested skills (next session should load)

- `loop-constraints` → `loop-budget` → `loop-triage` (in order) — any loop run
- `handoff` — if user asks for another handoff
- `diagnose` — if a loop/script misbehaves (harness + bash -n + shellcheck are the repro path)
- `write-a-skill` — for any new loop/agent-tools skill work
- `customize-opencode` — if background opencode sessions need config/permission changes
- `review`-adjacent: `weekly_review.sh` manual run = `bash ~/projects/agent-tools/scripts/weekly_review.sh`

## Artifacts to reference (do not duplicate)

- Spec: khelam `docs/sandbox/gemini-code-1786370796569.md` (per review-memory OA)
- Review memory: `/Users/rubenk/projects/khel-service/khelam/docs/reviews/review-memory.md` (Open Actions table — check BEFORE cost-flagged work)
- Session log: `/Users/rubenk/projects/khel-service/khelam/docs/sessions/2026-08-11.md`
- Tasklog: `/Users/rubenk/projects/khel-service/khelam/docs/tasklog.md`
