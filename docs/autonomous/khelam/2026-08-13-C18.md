# C18 — Caveman Console-Only Scope

**Card**: C18 (autonomous queue)
**Scope**: agent-tools
**Date**: 2026-08-13
**Status**: ✅ Complete (PR manual step)

## ✅ Complete

1. **Caveman skill modified with console-only scope** — `skills/caveman/SKILL.md` added to agent-tools repo with:
   - `## Scope (console-only)` section defining output channel rules:
     - Chat/console replies → caveman compression (terse)
     - File writes (.md, .txt, etc.) → full prose (uncompressed)
     - External messages (Discord, Slack, email) → full prose (uncompressed)
     - Code blocks → unchanged
   - Output channel decision table
   - Silent channel transitions (no announcement needed)

2. **Data collection methodology added** — `## Data Collection` section in SKILL.md:
   - Methodology: compare console reply tokens vs full-prose equivalent
   - Expected savings: ~60-75% on console output, 0% on files
   - Three measurement options documented (agent self-report, orchestration instrumentation, post-hoc opencode.db analysis)

3. **Impact check script created** — `scripts/caveman_impact_check.sh`:
   - 3 built-in sample pairs (prose vs caveman equivalents)
   - Word-count × 1.3 token proxy
   - Measured results: ~83% token savings on console output
   - Supports external sample files

4. **Committed + pushed** — branch `feat/c18-caveman-console-only` in `rubenkaksh/agent-tools`:
   - Commit `d5a93cd`: 2 files, 164 insertions
   - Pushed to origin

## ⬜ Remaining

1. **Draft PR creation** — `gh` CLI has no auth configured (`gh auth login` needed). Branch is pushed; PR command ready:
   ```bash
   gh pr create --draft \
     --title "feat(caveman): console-only scope + impact check script (C18)" \
     --body "## C18 — Caveman Console-Only Scope ..."
   ```

2. **Live data collection** — The script provides baseline estimates (~83% savings). Full orchestration-level token/cost data requires running caveman mode in actual sessions and comparing `opencode.db` before/after.

## 🧭 Next step

Run `gh auth login` then create the draft PR from the pushed branch.

## Findings

### Architecture
- `~/.agents/skills/caveman/` is NOT inside a git repo (standalone directory)
- `~/.claude/skills/caveman/` is a symlink to `~/.agents/skills/caveman/` (same files)
- The caveman skill was not previously in the agent-tools repo — it's now added at `skills/caveman/`
- Permission deny on `~/.agents/skills/**` prevented direct modification of the installed skill; solution was to add the canonical version to agent-tools repo

### Token Impact (baseline from script)
| Sample | Prose tokens | Caveman tokens | Savings |
|---|---|---|---|
| Auth middleware explanation | 67 | 15 | 77% |
| DB connection pooling | 71 | 14 | 80% |
| React re-render explanation | 100 | 11 | 89% |
| **Total** | **238** | **40** | **83%** |

Note: word-count × 1.3 is a rough proxy. Precise counts require a tokenizer or post-hoc `opencode.db` analysis.

## 📣 Completion summary
- 1. **Done** — Caveman skill with console-only scope + impact check script committed to `feat/c18-caveman-console-only` in agent-tools (~83% console token savings baseline)
- 2. **Concerns & notes** — `gh` not authenticated; draft PR needs `gh auth login` + manual creation. Installed skill at `~/.agents/skills/caveman/` NOT modified (permission deny) — agent-tools repo now has the canonical version.
- 3. **Next for user** — Run `gh auth login` then `gh pr create --draft` from the pushed branch; optionally sync the modified SKILL.md to `~/.agents/skills/caveman/` to activate console-only mode.
