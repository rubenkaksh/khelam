# L3 Test Window — khelam

- **Grant**: `.opencode/opencode.json` (all 14 tools allow, NO `external_directory` key — the global deny-list stays authoritative).
- **Status**: DESIGNATED test workspace for the `~/projects/sandbox/` migration (user, 2026-08-07).
- **revoke_at**: `2026-08-21`
- **Purpose**: test L3-without-prompt behavior on a real workspace while the sandbox (`~/projects/sandbox/`) becomes the default-L3 home.
- **Auto-flag**: the weekly review checks `revoke_at` and surfaces expired test grants for user sign-off (renew, revoke, or migrate the workspace into `sandbox/workspaces/`).
