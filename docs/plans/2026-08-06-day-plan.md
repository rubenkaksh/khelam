# Day Plan — 2026-08-06 (EXECUTION CONTRACT)

> **EXECUTION CONTRACT.** This is the ONLY document an execution session reads. Approved by: __________ (user signature below). Any deviation must be logged in the learnings log and escalated per the scopeshift protocol (status file `[scopeshift]` line).

## 0. Approval

- [x] User approved this plan as a whole
- [x] User approved trust dial assignments
- [x] User approved scope manifests per batch
- Signed: __user (a)__ Date: 2026-08-06

## 1. Context (carried forward, not re-derived)

### 1.1 Repositories (ABSOLUTE PATHS ONLY — verified 2026-08-06 08:xx)

| Name | Path | Branch | Dirty? | Key constraint |
|------|------|--------|--------|----------------|
| khelam | /Users/rubenk/projects/khel-service/khelam | main | untracked docs/sessions/2026-08-06.md (expected — will be committed) | pre-commit gate: analyze+test, docs-only skip |
| commons | /Users/rubenk/projects/commons | main | DIRTY: graphify-out/* modified (expected) | Do NOT stage graphify-out/ (denylisted) |
| forkable | /Users/rubenk/projects/forkable | test/aider | clean | future source of truth; samseer wired |
| backend | /Users/rubenk/projects/khel-service/rms-futsal-backend | feature/login-auth | dirty: src/config/env.config.ts (user's edit — leave) + untracked .opencode/plans/messenger-webhook-plan.md (leave) | DO NOT pkill :8000; DO NOT touch env.config.ts |

### 1.2 Running Services (DO NOT kill)

- Backend: port 8000 (PID 9903, log /tmp/rms-backend.log) — DO NOT pkill
- launchd com.khelam.weekly-review: loaded (Sunday 18:00) — DO NOT touch plist
- Emulator: emulator-5554 (only for integration tests if live path changes — not today's scope)
- opencode.db: /Users/rubenk/.local/share/opencode/opencode.db (table `session`)

### 1.3 Key Files (absolute paths)

- Handoff TODO (source of truth for steps): /Users/rubenk/projects/khel-service/khelam/docs/handoff-v1-execution.md
- Execution model spec: /Users/rubenk/projects/khel-service/khelam/docs/superpowers/specs/2026-08-05-background-agent-execution-model-design.md
- Sandbox/retry spec: /Users/rubenk/projects/khel-service/khelam/docs/superpowers/specs/2026-08-05-scope-sandbox-and-retry-policy-design.md
- Session file (write to): /Users/rubenk/projects/khel-service/khelam/docs/sessions/2026-08-06.md
- Status file (write per-batch): /Users/rubenk/projects/khel-service/khelam/docs/sessions/2026-08-06-status.md
- Review memory: /Users/rubenk/projects/khel-service/khelam/docs/reviews/review-memory.md (Open Actions: NONE as of 08-05 — re-check before closeout work)
- Global AGENTS.md: /Users/rubenk/.config/opencode/AGENTS.md (LIVE — governs all sessions; DO NOT modify)
- Violations log: /tmp/sandbox-violations.log (append-only; evening review reads)

### 1.4 opencode.db schema (verified 2026-08-05)

```
session: id, title, directory, agent, model(JSON {"id","providerID","variant"}),
  cost(FLOAT, always 0.0 for free tiers), tokens_input, tokens_output,
  tokens_reasoning, tokens_cache_read, tokens_cache_write,
  time_created(INTEGER EPOCH MS), time_updated(INTEGER EPOCH MS)
```
- LIKE matching for khelam dirs: `%/khelam` OR `%khel-service/khelam%`
- ccusage: `npx ccusage opencode session -j` — pricing join by sessionId (no per-repo field)

## 2. Rules the Agent MUST Follow

- Governed by the LIVE global AGENTS.md (Flutter `!`/`as T` ban, Cost Discipline, Work Tracking, sidetrack guard).
- Cost-note >5k-token ops in session file BEFORE (estimate) + AFTER (actual).
- DO NOT modify ~/.config/opencode/AGENTS.md. DO NOT redo Done items. DO NOT stage graphify-out/ in ANY repo. DO NOT touch launchd plist. DO NOT touch backend env.config.ts or messenger-webhook-plan.md.
- Never commit with known failures (pre-commit gate enforces). Docs-only commits skip the gate.
- Session Objective convention: `Feature: docs/features/<name>/README.md — <task>` or plain `<task>` (for collector feature_parent column).
- Failure policy (v1 = LOG-ONLY): on any failure, log `[FAIL ...]` line in learnings format (see §6), wait 1 min, retry (max 4 retries = 5 attempts, per-step budget, resets on acceptance-bar success). 2-strike identical-error rule: same sha256 sig twice → persistent → stop retrying, log, escalate per trust level (L1: halt for user; L2/L3: mark blocked per design §4.5). Do NOT spawn scout sessions today (deferred after v1).
- Sandbox (LIVE today): scope manifests §3 per batch; scope_guard.sh blocks out-of-scope commits; sandbox_audit.sh at SESSION BOUNDARY; post-commit backstop installed by Batch 2 setup.

## 3. Batches (THE EXECUTABLE PLAN)

> Each batch = one fresh opencode session. Mark "SESSION BOUNDARY" before starting. Trust dial per design (L1 halt + **15-minute idle rule: if an L1 halt fires and the user does not respond within 15 minutes, the agent proceeds to independent L2/L3 batches (per dependency graph §4), logs the idle duration in a learnings line, and returns to the halted batch when the user approves**; L2 auto-proceed, halt on deviations; L3 autonomous, escalate on blockers).

### Batch 1 — Day Plan + session file commit [TRUST: L1]
- **Objective**: Commit Day Plan contract + today's session file; establish status file.
- **Steps**:
  1. `git add docs/plans/2026-08-06-day-plan.md docs/sessions/2026-08-06.md` (khelam)
  2. `git commit -m "docs: day plan + session 2026-08-06 (background agent experiment)"`
  3. `git push origin main`
  4. Create `/Users/rubenk/projects/khel-service/khelam/docs/sessions/2026-08-06-status.md` with header + `current_batch=1`
  5. Write status line: `batch=1 status=done next=Batch 2 trust=L1`
- **Acceptance Bar (DONE =)**: commit on origin/main; status file exists with batch 1 done.
- **Cost Estimate (agent)**: ~500 tokens. (user): ____
- **Dependencies**: none
- **Scope manifest**: repos [khelam]; allow files [docs/plans/2026-08-06-day-plan.md, docs/sessions/2026-08-06*.md]; deny [lib/**, graphify-out/**, .env*, *.code-workspace]; commands [git add/commit/push]; commits pattern "docs: day plan*"; force-push: NO
- **SESSION BOUNDARY**

### Batch 2 — Sibling repos AGENTS.md [TRUST: L1]
- **Objective**: commons AGENTS.md deleted; forkable + backend marker files created + committed/pushed.
- **Steps** (per handoff Step B):
  1. commons: `git -C /Users/rubenk/projects/commons rm AGENTS.md` → commit ONLY that deletion → push main. WARNING: graphify-out/ is dirty — git rm stages only AGENTS.md; verify `git -C /Users/rubenk/projects/commons status --short` shows ONLY the AGENTS.md deletion before committing.
  2. forkable (test/aider): create `/Users/rubenk/projects/forkable/AGENTS.md` — 3-line marker (see handoff §3 Step B.2 for verbatim content) → commit → push.
  3. backend (feature/login-auth): create `/Users/rubenk/projects/khel-service/rms-futsal-backend/AGENTS.md` — marker with `# non_flutter: true` (verbatim in handoff) → commit → push. Do NOT stage env.config.ts or .opencode/.
  4. Validate: `flutter analyze` in commons + forkable (verify `!` ban compliance).
- **Acceptance Bar**: 3 repos show correct AGENTS.md state on remote; analyze clean in commons + forkable.
- **Self-elevation triggers** (any → ELEVATED TO L1, halt): `git rm` (destructive), staging graphify-out/ dirt, touching env.config.ts/.opencode/, force-push.
- **Cost Estimate (agent)**: ~2k tokens. (user): ____
- **Dependencies**: Batch 1 (recovery baseline)
- **Scope manifest**: repos [commons, forkable, backend]; allow files [AGENTS.md (repo roots only)]; deny [graphify-out/**, lib/**, .env*, env.config.ts, .opencode/**, src/**, any file not AGENTS.md]; commands [git rm, git add/commit/push, flutter analyze]; force-push: NO
- **SESSION BOUNDARY**

### Batch 3 — Analytics pipeline (Step C) [TRUST: L2]
- **Objective**: `~/analytics/` structure + `scripts/ccusage_collect.sh` + `scripts/analytics_charts.py` + `~/analytics/charts.md`.
- **Steps** (per handoff Step C, verbatim schemas):
  1. `mkdir -p ~/analytics/{weekly,monthly,charts}`
  2. Write `/Users/rubenk/projects/khel-service/khelam/scripts/ccusage_collect.sh` (sqlite3 query, LIKE matching `%/khelam` OR `%khel-service/khelam%` OR `%/forkable` OR `%/commons`; weekly CSV schema exact: `date,session_id,agent,model_name,input_tokens,output_tokens,cache_read_tokens,cache_write_tokens,reasoning_tokens,total_tokens,cost_usd,repo,title,duration_seconds,feature_parent`; monthly rollup schema exact per handoff; graceful degradation → warning to /tmp/weekly-review.log + fallback)
  3. Write `/Users/rubenk/projects/khel-service/khelam/scripts/analytics_charts.py` (8 charts, `--chart <n>`, PNGs → ~/analytics/charts/, graceful matplotlib-missing exit with hint)
  4. Write `~/analytics/charts.md` (chart recipes doc — CSV source, columns, chart type, question, snippet; GLOBAL ~/analytics/ per decision, NOT in repo)
  5. RUN: `bash scripts/ccusage_collect.sh` → verify ≥1 row in ~/analytics/weekly/2026-08-06.csv
- **Acceptance Bar**: collector script runs and produces weekly CSV with correct columns; charts.py runs with `--chart 1` (or graceful matplotlib hint); charts.md exists.
- **Cost Estimate (agent)**: ~6k tokens. (user): ____
- **Dependencies**: Batch 1
- **Scope manifest**: repos [khelam]; allow files [scripts/ccusage_collect.sh, scripts/analytics_charts.py, ~/analytics/**]; deny [lib/**, graphify-out/**, .env*, *.code-workspace, docs/** (except writing learnings log to session file)]; commands [mkdir, bash, python3, sqlite3, npx ccusage, git add/commit/push]; force-push: NO
- **SESSION BOUNDARY**

### Batch 4 — weekly_review.sh v2 + report_sink.sh (Step D) [TRUST: L2]
- **Objective**: upgrade weekly review script + new report sink; preserve existing pieces.
- **Steps** (per handoff Step D):
  1. Prepend `ccusage_collect.sh` invocation (step 1 before session selection)
  2. Evolve review-agent PROMPT with SECTION A (Analytics → performance-summary.md, exact structure per handoff), SECTION B (Update log → ~/analytics/update-log.md), SECTION C (Feature audit); review doc gains Performance Summary + Feature Audit sections appended after existing
  3. Write `/Users/rubenk/projects/khel-service/khelam/scripts/report_sink.sh` (`send_report <title> <body> [artifact...]`, REPORT_SINK env: macos_notification default / discord_webhook / slack_webhook stubs / noop)
  4. Wire into weekly_review.sh replacing inline osascript; keep FAILED fallback notification
  5. Preserve: filename-date session selection, commons consumer check, codegraph/graphify tripwire
  6. `bash -n` both scripts
- **Acceptance Bar**: bash -n clean; script structure verified (read-through: collector prepended, 3 prompt sections present, report_sink wired, preserved pieces present).
- **Cost Estimate (agent)**: ~7k tokens. (user): ____
- **Dependencies**: Batch 3 (collector must exist first)
- **Scope manifest**: repos [khelam]; allow files [scripts/weekly_review.sh, scripts/report_sink.sh]; deny [lib/**, graphify-out/**, .env*, *.code-workspace, ~/analytics/** (written by scripts at run time, not edited), docs/** except session file learnings]; commands [bash -n, git add/commit/push, vim-less edits]; force-push: NO
- **SESSION BOUNDARY**

### Batch 5 — Feature doc migration (Step E) [TRUST: L2]
- **Objective**: migrate `docs/prd/booking-calendar-feature.md` → `docs/features/booking-calendar/README.md`.
- **Steps** (per handoff Step E):
  1. Create `/Users/rubenk/projects/khel-service/khelam/docs/features/booking-calendar/README.md` from the PRD (template: Problem Statement / Scope In-Out / User Stories / Architecture Decisions table / Data Models / Implementation Plan checklist w/ acceptance bars / Test Plan / Progress Tracker / Backlinks; ≤200 lines)
  2. Update links in session files + backlog.md referencing old path; one-line redirect in docs/prd/ or delete after sweep
  3. Note in docs/backlog.md: docs/superpowers/ stays as-is (not declared features)
  4. Document Session Objective convention in khelam AGENTS.md or session template (per handoff E.4 — NOTE: khelam AGENTS.md is the slimmed project file; add the convention there if it fits, else session template section in global is off-limits → add to project AGENTS.md)
- **Acceptance Bar**: README exists ≤200 lines w/ all template sections; no stale links to old path (grep); convention documented.
- **Cost Estimate (agent)**: ~3k tokens. (user): ____
- **Dependencies**: none (independent of 3/4)
- **Scope manifest**: repos [khelam]; allow files [docs/features/booking-calendar/**, docs/prd/**, docs/backlog.md, docs/sessions/2026-08-06.md, AGENTS.md (khelam root, convention line only)]; deny [lib/**, graphify-out/**, .env*, scripts/**]; commands [git add/commit/push, grep]; force-push: NO
- **SESSION BOUNDARY**

### Batch 6 — Validation (Step F) [TRUST: L1] ← HARD GATE for evening review
- **Objective**: run all acceptance checks end-to-end.
- **Steps** (per handoff Step F):
  1. `bash -n` on all new/edited scripts
  2. `bash scripts/ccusage_collect.sh` → rows appear in ~/analytics/weekly/
  3. `bash scripts/weekly_review.sh` end-to-end → 4 artifacts exist (docs/reviews/YYYY-MM-DD.md, weekly CSV, ~/analytics/performance-summary.md, ~/analytics/update-log.md) + notification fired + nothing committed by the script
  4. `python3 scripts/analytics_charts.py --chart 1` → PNG in ~/analytics/charts/
  5. `flutter analyze` in khelam clean
- **Acceptance Bar**: ALL 5 checks pass. This is the gate: evening review cannot start without ≥1 CSV row in ~/analytics/weekly/.
- **Cost Estimate (agent)**: ~5k tokens. (user): ____
- **Dependencies**: Batches 3, 4, 5
- **Scope manifest**: repos [khelam]; allow files [scripts/**, docs/sessions/2026-08-06*.md, ~/analytics/**]; deny [lib/**, graphify-out/**, .env*, *.code-workspace]; commands [bash -n, bash scripts/**, python3 scripts/**, flutter analyze]; commits: NONE expected (validation only; if a fix commit is needed → ELEVATE TO L1)
- **SESSION BOUNDARY**

### Batch 7 — Closeout (Step G) [TRUST: L1]
- **Objective**: update-log entries, review-memory update, commits across repos, actual-cost log, final report.
- **Steps** (per handoff Step G):
  1. Append update-log entries to `~/analytics/update-log.md` (create with header if missing) for EVERY change made this session (dual-author rule)
  2. Update `/Users/rubenk/projects/khel-service/khelam/docs/reviews/review-memory.md`: implemented-measures rows (hierarchy v2, analytics pipeline, feature PRD model, report_sink) + Review History note
  3. Commit + push: khelam (main), forkable (test/aider), commons, backend
  4. Log ACTUAL cost vs ~35k estimate in session file cost-note line
  5. Write final report in session file (what shipped per repo, artifacts, decisions honored, deviations + why, what remains for v2)
- **Acceptance Bar**: all 4 repos pushed; update-log + review-memory updated; cost-note actual logged; final report written. Evening review session THEN starts (Phase 3).
- **Cost Estimate (agent)**: ~4k tokens. (user): ____
- **Dependencies**: Batches 2, 4, 5 (their commits), 6
- **Scope manifest**: repos [khelam, commons, forkable, backend]; allow files [~/analytics/update-log.md, docs/reviews/review-memory.md, docs/sessions/2026-08-06*.md, AGENTS.md (already-committed markers — verify only)]; deny [lib/**, graphify-out/**, .env*, env.config.ts, .opencode/**, src/**]; commands [git add/commit/push (specific paths only)]; force-push: NO
- **SESSION BOUNDARY**

## 4. Inter-Batch Dependencies

```
B1 → B2 → (independent: B3, B4 [needs B3], B5) → B6 → B7
```
- B6 depends on B3 + B4 + B5. B7 depends on B2 + B4 + B5 + B6.
- B3, B5 independent of each other and of B2 → dependency-aware scheduling: if an L1 halt blocks B2, B3/B5 may proceed.

## 5. Output Locations

- Learnings log: inline in session file Work Log, one-line format §6
- Status updates: /Users/rubenk/projects/khel-service/khelam/docs/sessions/2026-08-06-status.md (one line per batch: `batch=N step=<tag> status=<done|partial|blocked|elevated> tokens_est=(agent,user) next=<step> trust=<L1|L2|L3> scope=<clean|dirty>`)
- Evening review input: this Day Plan + session file + status file + ~/analytics/weekly/2026-08-06.csv + /tmp/sandbox-violations.log

## 6. Learnings + Failure Log Format (one line per checkpoint, greppable)

```
[CHECKPOINT] batch=<N> trust=<L1|L2|L3> status=<done|partial|blocked|elevated> tokens_in=<est,actual> tokens_out=<est,actual> drift=<count> blockers=<comma-sep|none> gut_check=<user_vs_actual|skipped> scope=<clean|dirty>
[FAIL] batch=<N> step=<tag> model=<primary> attempt=<n/5> kind=<transient|persistent|acceptance-bar> sig=<4hex> action=<retry|escalate> wait=60s ts=<HH:MM>
```
- `sig` = first 4 hex of sha256(stderr+exit-code). Same sig twice = persistent = stop retrying (2-strike rule).

## 7. Evening Review Prerequisites (Phase 3 — check BEFORE starting)

- [ ] Batch 6 acceptance bar met (bash -n green; collector CSV has ≥1 row; weekly_review.sh produced 4 artifacts; chart PNG exists; flutter analyze clean)
- [ ] All commits pushed (4 repos)
- [ ] Status file shows batch 7 done
- [ ] I/O numbers available: ~/analytics/weekly/2026-08-06.csv exists
- [ ] /tmp/sandbox-violations.log read (empty or triaged)
- Fallback: if collector CSV missing → evening review counts tokens manually from session file cost-note lines (degraded mode, not a blocker)
